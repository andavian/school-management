info:
  name: Listar libros de calificaciones
  type: http
  seq: 7

http:
  method: GET
  url: "{{baseUrl}}/api/admin/qualification-registries?status=CLOSED"
  params:
    - name: status
      value: CLOSED
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
