info:
  name: Listar libros de calificaciones
  type: http
  seq: 7

http:
  method: GET
  url: |-
    GET {{baseUrl}}/api/admin/qualification-registries?status=CLOSED
    p
    GET {{baseUrl}}/api/admin/qualification-registries?status=CLOSED
  params:
    - name: status
      value: |-
        CLOSED
        p
        GET {{baseUrl}}/api/admin/qualification-registries?status=CLOSED
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
