info:
  name: 04 - PATCH Corregir Asistencia
  type: http
  seq: 4

http:
  method: GET
  url: "{{baseUrl}}/api/attendance/course/{{course_attendance_id}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
