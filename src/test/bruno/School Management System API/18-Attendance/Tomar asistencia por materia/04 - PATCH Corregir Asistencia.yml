info:
  name: 04 - PATCH Corregir Asistencia
  type: http
  seq: 4

http:
  method: GET
  url: "{{baseUrl}}/api/attendance/course/{{course_attendance_id}}"
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("Corrige a ausente", function() {
            expect(res.status).to.equal(200);
            expect(res.getBody().status).to.equal("ABSENT");
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
