info:
  name: 03 - POST Asistencia por Materia
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/api/attendance/course"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
