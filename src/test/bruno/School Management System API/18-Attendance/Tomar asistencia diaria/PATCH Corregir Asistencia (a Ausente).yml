info:
  name: 03 - PATCH Corregir Asistencia (a Ausente)
  type: http
  seq: 3
  tags:
    - Attendance

http:
  method: PATCH
  url: "{{baseUrl}}/api/attendance/daily/{{daily_attendance_id}}"
  body:
    type: json
    data: |-
      {
        "newStatus": "ABSENT",
        "observations": "El alumno no asistiÃ³"
      }
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("Corrige asistencia a ausente", function() {
            expect(res.status).to.equal(200);
            var body = res.getBody();
            expect(body.status).to.equal("ABSENT");
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5






