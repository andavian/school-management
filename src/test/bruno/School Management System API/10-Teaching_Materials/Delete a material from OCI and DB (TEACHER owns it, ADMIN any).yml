info:
  name: Delete a material from OCI and DB (TEACHER owns it, ADMIN any)
  type: http
  seq: 2
  tags:
    - Teaching_Materials

http:
  method: DELETE
  url: "{{baseUrl}}/api/materials/:materialId"
  params:
    - name: materialId
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/api/materials/:materialId"
      method: DELETE
      params:
        - name: materialId
          value: ""
          type: path
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
